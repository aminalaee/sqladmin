---
description: 
globs: 
alwaysApply: true
---
# Testing Conventions

## Running Tests

The only approved command for running tests in this project is:

```bash
hatch run test:test
```

Do not use alternative test commands like `pytest` directly or other test runners. This ensures consistent test execution across the development team.
